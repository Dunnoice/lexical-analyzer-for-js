while (!fin+eof())
{
    %%%=
    line_count++;
    getline(fin, line);

    line += "\n";

    for (int i = 0, length = line length(); i < length; i++)
    {
        checkSymbol(string(1, line));
    }
}